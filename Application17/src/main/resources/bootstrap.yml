spring:
  application:
    name: application17
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        instanceId: ${spring.application.name}
      config:
        failFast: false
        format: yaml

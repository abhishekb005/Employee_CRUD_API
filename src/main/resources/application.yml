spring:
  application:
    name: application1
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        instanceId: ${spring.application.name}
      config:
        failFast: false
        format: yaml
